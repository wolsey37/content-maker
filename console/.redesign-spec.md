# 재설계 스펙: Studio Console — Calm Instrument · Glowing Media (계기판/암실 이원화 + 신호=상태 + 비용 어웨어)

## tokensCss

```css
/* ===== 기존 변수명 100% 보존 + 값 개선 + 신규 토큰 추가 =====
   원칙: --radius/--shadow/--shadow-lg 등은 .panel/.card/details.fold/.hero/.notready 가
   직접 소비하므로 '값만 재정의', 절대 삭제 금지. 신규 --r-*/--s*/--t-*/--sh-* 는
   componentSpecs 가 셀렉터를 새 토큰으로 재배선해야 실제 적용됨(미배선 시 무효). */
:root{
  /* --- 표면: 회청 저채도 '계기판' --- */
  --bg:#f3f5f8; --panel:#ffffff; --ink:#18212e; --muted:#6a7686;
  --line:#e2e7ee; --line-soft:#eef1f5;
  --line-strong:#cfd6e0;                 /* 신규: 강한 경계·호버 보더 */
  --ink-2:#3a4654;                        /* 신규: 본문 보조 잉크 */
  /* --- 브랜드: 신호 전용(살짝 더 단단) --- */
  --brand:#3b5bdb; --brand-d:#2f49b8; --brand-soft:#eef2fe;
  --brand-line:#c5d0f7;                   /* 신규: llm-pill 등 브랜드 보더 */
  /* --- 상태색(녹=완료/적=에러/황=경고) 보존+소폭 정비 --- */
  --ok:#12915a; --ok-soft:#e7f6ee; --ok-line:#b9e3cd;
  --err:#d23b32; --err-soft:#fcebe9; --err-line:#f0c0bb;
  --warn:#a9760a; --warn-soft:#fbf3e1; --warn-line:#ecd59a;
  --info:#0e7490; --info-soft:#e0f5f9; --info-line:#b6e3ec;   /* 신규: .note info 변형 */
  /* --- 보조 표면 --- */
  --head-bg:#f8fafc; --hover:#f1f3f7; --code-bg:#f6f8fb; --code-ink:#3a4654;
  --chip-bg:#eef1f5; --input-bg:#ffffff; --topbar-bg:rgba(255,255,255,.85);
  --panel-2:#f8fafc;                      /* 신규: 카드내 미디어 stage 단차 배경 */
  /* --- 그림자: 3단계 + 미디어 --- */
  --sh-1:0 1px 2px rgba(16,24,40,.05);                 /* 평면 카드 */
  --sh-2:0 6px 18px rgba(16,24,40,.10);                /* 드롭다운·호버 부상 */
  --sh-3:0 18px 50px rgba(16,24,40,.22);               /* 모달·드로어·라이트박스 */
  --sh-media:0 4px 16px rgba(8,12,20,.16);             /* 신규: 발광 미디어 */
  --shadow:var(--sh-1);                    /* 기존명 → 신스케일로 재배선(소비처 자동 정렬) */
  --shadow-lg:var(--sh-3);
  /* --- 라운드: 4단계(기존 --radius 값만 13→14, 신규 스텝 추가) --- */
  --r-sm:7px;     /* 칩 내부·태그·작은 버튼 */
  --r-md:10px;    /* 버튼·입력·셀렉트 */
  --r-lg:14px;    /* 카드·패널·드로어·모달 */
  --r-pill:999px;
  --radius:var(--r-lg);                    /* 기존명 보존: .panel/.card/.hero/.notready 자동 정렬 */
  /* --- 타이포 스케일: 6단계(신규) --- */
  --t-cap:11px;   /* 대문자 라벨·메타·키커 */
  --t-sm:12.5px;  /* 보조·hint */
  --t-body:13.5px;/* 본문·입력·버튼 */
  --t-h3:15px;    /* 카드 제목·드로어 */
  --t-h2:18px;    /* 섹션 */
  --t-h1:24px;    /* 본문 페이지 제목(jobs-title) */
  --fw-body:500; --fw-strong:600; --fw-title:700; --fw-display:800;  /* 굵기 4단계 수렴 */
  /* --- 간격: 8 기반(신규) --- */
  --s1:4px; --s2:8px; --s3:12px; --s4:16px; --s5:24px; --s6:32px; --s7:48px;
  /* --- 모션(신규) --- */
  --ease:cubic-bezier(.4,0,.2,1); --dur-fast:120ms; --dur:220ms;
  --focus-ring:0 0 0 3px var(--brand-soft);  /* 기존 포커스 링 토큰화 */
}
html[data-theme="dark"]{
  /* --- '암실': 진짜 어둡되 푸른기 제거한 중성 잉크 --- */
  --bg:#0c1015; --panel:#151b23; --ink:#e8edf3; --muted:#8693a1;
  --line:#232d39; --line-soft:#1e2731;
  --line-strong:#33404e; --ink-2:#b3bdc9;
  --brand:#6f8bff; --brand-d:#5d78ef; --brand-soft:#18233c; --brand-line:#34467a;
  --ok:#44cf8d; --ok-soft:#0f2b1d; --ok-line:#1f5b3c;
  --err:#ff6b60; --err-soft:#341a17; --err-line:#7a302a;
  --warn:#e7b24a; --warn-soft:#2e2613; --warn-line:#5c4a1e;
  --info:#4cc4d6; --info-soft:#0d2a30; --info-line:#1c4a52;
  --head-bg:#1b232d; --hover:#212b37; --code-bg:#111923; --code-ink:#b7c2cf;
  --chip-bg:#222d3a; --input-bg:#0f161e; --topbar-bg:rgba(16,22,30,.85);
  --panel-2:#1b232d;
  /* ★다크 가독성 수정: 떠있는 표면 알파를 .18→.55+ 로(login-card/app-modal/viewer 묻힘 해소) */
  --sh-1:0 1px 2px rgba(0,0,0,.4);
  --sh-2:0 6px 20px rgba(0,0,0,.5);
  --sh-3:0 20px 56px rgba(0,0,0,.62);
  --sh-media:0 4px 16px rgba(0,0,0,.5), 0 0 0 1px rgba(120,150,255,.12);  /* 다크 미디어 발광 */
  --shadow:var(--sh-1); --shadow-lg:var(--sh-3);
}
@media (prefers-reduced-motion: reduce){
  *{ animation-duration:.001ms !important; transition-duration:.001ms !important; }
}
```

## componentSpecs

### 버튼 .btn (라운드/타이포 재배선)
- selectors: .btn / .btn.primary / .btn.ghost / .btn.danger / .btn.sm
```css
.btn{ border-radius:var(--r-md); font-size:var(--t-body); font-weight:var(--fw-strong); transition:background-color var(--dur-fast),border-color var(--dur-fast),transform .05s; } .btn.sm{ border-radius:var(--r-sm); font-size:var(--t-sm); } .btn.ghost:hover{ border-color:var(--line-strong); background:var(--hover); } .btn.primary{ box-shadow:var(--sh-1); }
```
> note: 기존 9px 하드코딩 → --r-md. .btn은 --radius를 안 읽으므로 반드시 명시 재배선해야 신규 토큰이 적용됨. :active translateY(1px)·:disabled·focus-visible 링은 보존.

### 입력/셀렉트/textarea (라운드/타이포/포커스링)
- selectors: input[type=text],input[type=password],input[type=number],select,textarea
```css
input[...],select,textarea{ border-radius:var(--r-md); font-size:var(--t-body); } input:focus,select:focus,textarea:focus{ box-shadow:var(--focus-ring); border-color:var(--brand); }
```
> note: 기존 9px→--r-md. .mono 12.5px·textarea resize:vertical 보존. 포커스 링을 --focus-ring 토큰으로 통일(값 동일).

### 칩 .chip (신호=신뢰: 비선택 무채, 선택만 브랜드)
- selectors: .chip / .chip:hover / .chip.sel
```css
.chip{ border-radius:var(--r-pill); font-size:var(--t-body); background:var(--panel); border-color:var(--line); } .chip:hover{ border-color:var(--line-strong); } .chip.sel{ background:var(--brand); border-color:var(--brand); color:#fff; }
```
> note: 콘텐츠 종류·참조 사이트 공용 클래스이므로 구조 보존. '선택됨'만 채워 신호로. 종류칩 hit-target은 패딩 var(--s2) var(--s4)로 약간 키움.

### 상태칩 .conn-pill / .llm-pill + modeBadge 정직화(#11)
- selectors: .conn-pill / .conn-pill.ok / .bad / .checking / .llm-pill / .top-actions #modeBadge
```css
.conn-pill{ border-radius:var(--r-pill); font-size:var(--t-sm); } .conn-pill.llm-pill{ border-color:var(--brand-line); } .top-actions #modeBadge{ cursor:default; border-style:dashed; background:transparent; color:var(--muted); }
```
> note: ok/bad/checking 색·도트·pulse 보존(신호언어 핵심). modeBadge는 읽기전용이므로 점선+무채+cursor default로 '클릭 불가' 어포던스 정직화. #connPill/#llmInfo만 채워진 알약.

### 단계 카드 .card (좌측 상태바·점등 전이)
- selectors: .card / .card.state-loading / .state-done / .state-error / .card-head .num / .chip
```css
.card{ border-radius:var(--r-lg); box-shadow:var(--sh-1); transition:border-color var(--dur-fast),box-shadow var(--dur-fast); } .card .num,.card .chip,.card.state-done .num{ transition:background-color 200ms var(--ease),color 200ms var(--ease); }
```
> note: 좌측 4px 컬러바·state-* 클래스·num✓치환·chip.loading/done/error 전부 보존(JS 의존). done 전이에 200ms 색 '점등' 모션만 추가. --radius가 --r-lg를 가리키므로 카드 라운드는 자동 정렬.

### 카드 헤더 정보밀도 해소(#8) — 보수형 CSS-only
- selectors: .card-head / .card-head .step-detail-btn / .card-head .desc
```css
.card-head{ gap:var(--s3); padding:var(--s3) var(--s4); flex-wrap:wrap; } .card-head .title{ font-size:var(--t-h3); font-weight:var(--fw-title); } .card-head .desc{ font-size:var(--t-sm); } .card-head .step-detail-btn{ border-radius:var(--r-sm); }
```
> note: renderSteps의 card-head 자식은 flat sibling(.num / [무클래스 flex wrapper: .title+.desc] / .step-detail-btn / .chip / .chev). 진짜 2행 그리드는 그 wrapper div에 class 부여(=renderSteps 마크업 1줄 추가, 허용범위)가 필요. CSS-only 보수형은 좁은폭서 wrap만 허용 + detail-btn은 이미 stopPropagation으로 클릭존 분리됨(1040)이라 충돌 없음. 2행 전환은 risks 참조.

### 안내박스 7종 통합(#6) — .note 별칭 패턴
- selectors: .warnbox,.scope-note,.danger-note,.missing-note,.notready,.lock-hint,.eff-hint (+ 신규 .note/.note-info/.note-warn/.note-danger)
```css
.note,.warnbox,.scope-note,.danger-note,.missing-note,.lock-hint,.eff-hint{ border-radius:var(--r-md); padding:var(--s3); font-size:var(--t-sm); border-left:4px solid var(--line); } .note-info,.scope-note,.lock-hint,.eff-hint{ background:var(--info-soft); border-left-color:var(--info); } .note-warn,.warnbox,.missing-note,.notready{ background:var(--warn-soft); border-left-color:var(--warn); } .note-danger,.danger-note{ background:var(--err-soft); border-left-color:var(--err); }
```
> note: 기존 7개 클래스명 전부 보존(JS가 토글/생성). 공통 토큰으로 외형만 1체계 수렴. 좌측 4px 의미색 바 = 신호언어와 동일. .notready는 라운드 --r-lg 유지.

### 패널·폴드·드로어·히어로 (깊이·라운드 정렬)
- selectors: .panel / details.fold / details.help / .drawer / .hero
```css
.panel,details.fold,details.help{ border-radius:var(--radius); box-shadow:var(--sh-1); } .drawer{ box-shadow:var(--sh-3); } .hero{ border-radius:var(--radius); box-shadow:var(--sh-1); }
```
> note: 전부 --radius/--shadow를 이미 소비 → 토큰 값 재정의만으로 자동 정렬(추가 작업 최소). 드로어는 --sh-3로 명시 격상.

### 갤러리(이미지/영상) — cssText 정직 처리(#2)
- selectors: renderGallery(1408~) / renderVideoGallery(1675~)의 fig/img/cap/links cssText
```css
신규 .media-tile{ border:1px solid var(--line); border-radius:var(--r-md); overflow:hidden; background:var(--panel-2); box-shadow:var(--sh-media); } .media-tile img,.media-tile video{ background:var(--code-bg); } .media-cap{ padding:var(--s1) var(--s2); }
```
> note: ★CSS-only 미도달 영역. 구현경로=el.style.cssText를 el.className으로 교체(createElement·addEventListener·src·aspect-ratio 분기 4:5/9:16·object-fit:contain/cover 보존). 추출 후 --sh-media+다크 글로우로 '발광 미디어' 실현. cssText 값 인플레이스 교체로도 가능(둘 중 택1, 본 스펙은 class 추출 권장).

### 이미지/영상 생성 컨트롤 + 비용 가드 바(#12) — cssText 정직 처리
- selectors: renderImageGenCard(1494~)·renderVideoGenCard(1772~)의 ctrl/settingsGroup/cntWrap/concWrap/cntInfo cssText, igEls.countInfo(1571), vgEls.countInfo(1667 updateVgCount)
```css
신규 .gen-ctrl{ gap:var(--s3); } .cost-bar{ display:flex; align-items:center; gap:var(--s2); margin-top:var(--s2); padding:var(--s2) var(--s3); border-radius:var(--r-md); background:var(--warn-soft); border:1px solid var(--warn-line); font-size:var(--t-body); font-weight:var(--fw-strong); } .cost-bar b{ color:var(--brand); }
```
> note: ★CSS-only 미도달. updateVgCount(1657)/igEls.countInfo는 이미 '컷 N개 중 M개 · ≈X크레딧' 텍스트를 출력 — 이 span에 .cost-bar class만 부여(textContent/innerHTML 로직 불변)해 생성버튼 옆 비용바로 격상. 동시처리·최대장수 input은 64px→정상폭으로 cssText 값 교체. 신규 .btn.cost는 genBtn className에 추가(이벤트 보존).

### 에러 복구 슬롯(#7) — 목록 로드 실패
- selectors: loadJobsList catch(2592) '목록 오류' innerHTML / status.error
```css
신규 .note-danger 안에 [다시 시도] 버튼 슬롯 표준화: '<div class="note note-danger">목록 오류 — 연결/인증을 확인하세요.<button class="btn sm" ...>다시 시도</button></div>'
```
> note: innerHTML 문자열에 버튼 마크업 추가 + 기존 loadJobsList/refreshConn 핸들러 재호출(신규 핸들러 불필요). status.error도 동일 패턴 확장 가능. 막다른 길 제거.

### 타이포 위계 정렬(#9)
- selectors: .jobs-title / .hero .hero-q / h1 / .panel h2
```css
.jobs-title{ font-size:var(--t-h1); font-weight:var(--fw-display); } .hero .hero-q{ font-size:var(--t-h2); font-weight:var(--fw-title); } h1{ font-size:var(--t-h2); } .panel h2{ font-size:var(--t-h3); }
```
> note: jobs-title 26→24(--t-h1), hero-q 17→18(--t-h2)로 위계 격차 완화. 굵기 600~880 6단계→4단계(--fw-*) 수렴.


## screenChanges

### 사이드바 (#sidebar)
구조 보존(브랜드+#contentListNav+#sideFoot). 폭 220 유지. 빈 공간 redeem은 '선택적 additive'로 격리(risks 참조) — 척추에서는 .side-link active 동기화·라운드 --r-md·로그아웃(.side-foot, margin-top:auto) 시각 정렬만. 760px 이하 가로바 전환 동작 보존. relocateNav가 logoutTopBtn을 sideFoot로 옮기는 로직 불변.

### 상단바 (.topbar / .top-actions)
과밀 8컨트롤(relocateNav가 modeBadge·connPill·jobsBtn·llmInfo·modeToggle·themeToggle를 #topActions로 이동; toggleAll·logoutTopBtn은 topbar-top 잔류)을 시각 2존으로: 읽기전용 상태칩(modeBadge·connPill·llmInfo)을 좌측 그룹 외형(modeBadge 점선·무채), 액션버튼을 우측. 칩 높이 34 정렬 보존. 편집 화면 #rail+progress-line은 상단바 하단 풀폭 띠 유지(세그 라운드 --r-sm·gap --s2). relocateNav 배열·id 절대 불변.

### 콘텐츠 목록 (라이브러리, body.lib-mode / #jobsPanel)
lib-mode 메커니즘(.wrap>*:not(#jobsPanel) display:none, #jobsBackdrop none, #jobsPanel visibility 되돌림) 그대로 사용 — 뷰 전환 로직 불변. jobs-hero(kicker+title24+sub)→jobs-toolbar(날짜필터+개수)→#jobsList 그리드(auto-fill minmax 300, gap --s4). .job-row 썸네일(220px·blur배경·contain)을 '암실' 주인공으로, 메타는 저채도. jobs-empty는 라운드 --r-lg·온보딩 카피 위계. 목록 로드 에러에 .note-danger+[다시 시도].

### 편집 히어로 (.hero / #g_topic / hero-run)
editorHero(제목)→notReady→hero(주제입력)→#rail→#steps. hero를 'Compose Bar'로: #g_topic 강조, 선택입력(웹자료·참조·분석기준 .hero-extra-panel)은 시각 노이즈 축소. hero-run의 ✨만들기(primary)·⏩전체자동(ghost) 위계 명확. #g_topic·#heroRun·#heroAuto·#refAddBtn 등 id 보존. inputs-locked(종류칩만 잠금) 동작 보존.

### 단계 카드 (renderSteps 산출 .card)
좌측 상태바·.num✓·.chip.loading/done/error·.collapsed/.detail-open/.deliverable·#card-img/#card-vid 보존. 헤더 정보밀도: title --t-h3·desc --t-sm·detail-btn 별도 클릭존(이미 stopPropagation 1040). done 점등 200ms. card-img/card-vid 갤러리 영역만 --panel-2 stage 배경으로 미디어 부각.

### 갤러리 (renderGallery / renderVideoGallery / 생성카드)
★cssText→class 추출 또는 값 인플레이스 교체(CSS-only 미도달). 미디어 타일 .media-tile에 --sh-media+다크 글로우, aspect-ratio 분기·object-fit 보존. 생성카드 비용바: igEls.countInfo/vgEls.countInfo span에 .cost-bar class 부여로 격상(updateVgCount 텍스트 로직 불변), 동시처리·최대장수 input 폭 정상화. element/event/id/src 전부 보존.

### 설정 드로어 (#drawer) / 작업 드로어(#jobsPanel)
#drawer 우측 슬라이드 440 보존, --r-lg+--sh-3. switch-row/drawer-section 라운드·간격을 스케일로 정리. #jobsPanel은 구조상 .drawer지만 본문화 유지(#jobsBackdrop none·visibility 되돌림 규칙 불변). drawer-head/close ✕ 숨김 규칙 보존. provider/defaultModel/modelMenu/refreshModelsBtn id 불변.

### 로그인 오버레이 (#loginOverlay / .login-card)
login-card 라운드 --r-lg + 그림자 --sh-3(하드코딩 0 10px 40px .18 → 토큰)로 다크 경계 가독성 수정(★핵심). login-title --t-h2·sub --t-sm 위계. 2FA 타이머(.tfa-timer) 강조 유지. #loginPwView/#login2faView/#loginId/#loginPw/#verify2faBtn/#tfaTimer 등 id·뷰 토글 불변. app-modal·image-viewer도 동일하게 토큰 그림자로 교체.
