{
  "name": "content-maker-console",
  "version": "1.0.0",
  "private": true,
  "description": "콘텐츠 파이프라인 콘솔 — 로컬(CLI) 진입점 bridge.mjs / 서버(API) 진입점 lambda.mjs",
  "type": "module",
  "main": "bridge.mjs",
  "scripts": {
    "start": "node bridge.mjs",
    "start:api": "BACKEND=api node bridge.mjs"
  },
  "comment": "런타임 코드는 fetch 만 사용(의존성 0). 아래 AWS SDK 는 STORAGE=s3 / Secrets Manager 사용 시에만 동적 import 되며, 로컬(STORAGE=local) 테스트엔 설치가 필요하고 Lambda 아티팩트엔 버전 고정을 위해 번들 권장.",
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/s3-request-presigner": "^3.700.0",
    "@aws-sdk/client-secrets-manager": "^3.700.0"
  }
}
